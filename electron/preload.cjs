'use strict';

// Preload runs in the renderer context before the page loads.
// Use contextBridge here if you need to expose Node/Electron APIs
// to the React app. Nothing is needed for the current feature set.
